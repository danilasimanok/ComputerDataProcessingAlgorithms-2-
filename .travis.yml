language: csharp

mono: none
dotnet: 3.1.101

before_install:
    - sudo apt-get install nuget

install:
    - nuget restore test/Tasks/Tasks.sln

script:
    - dotnet build test/Tasks/Tasks.sln
    - dotnet test test/Tasks/Tasks.sln